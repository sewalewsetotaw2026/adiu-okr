# Database Configuration
# Update these values to match your docker-compose or local setup
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=employee_management_db
DATABASE_URL="postgresql://postgres:password@localhost:5432/employee_management_db?schema=public"

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS (Production)
# Set this to your deployed frontend URL (Vercel, etc.).
# The backend uses this to allow browser requests from your frontend.
FRONTEND_URL=http://localhost:5173

# Optional: comma-separated allowlist of additional origins.
# Example: CORS_ORIGINS=https://kacha-hris.vercel.app,http://localhost:5173
CORS_ORIGINS=

# Temporary troubleshooting: allow any Origin (reflected).
# NOTE: Prefer FRONTEND_URL/CORS_ORIGINS allowlist for production.
CORS_ALLOW_ALL=false

# JWT Configuration
# CHANGE THIS TO A SECURE SECRET IN PRODUCTION
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d


# Email (SMTP)
# For Gmail, use an App Password (not your normal Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Optional but recommended (preferred by the code if set)
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="Kacha HR <noreply@yourdomain.com>"
# SMTP_SECURE=false

# These are used by the backend (fallback if SMTP_USER/SMTP_PASS are not set)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Frontend link used in credential email
EMPLOYEE_LOGIN_URL=http://localhost:5173/employee-login
PASSWORD_RESET_URL



# =====================================
# EMAIL_PROVIDER: Choose your email provider
# =====================================
# Options: "nodemailer" | "smtp" | "resend"
# - nodemailer/smtp: Use SMTP via Nodemailer
# - resend: Use Resend API
EMAIL_PROVIDER=nodemailer

# =====================================
# Resend Configuration
# =====================================
# Use Resend instead of SMTP
# EMAIL_PROVIDER=resend

# Resend credentials
RESEND_API_KEY=re_************************
RESEND_FROM="Kacha HR <onboarding@yourdomain.com>"  # must be a verified sender/domain in Resend

# Optional
RESEND_REPLY_TO=support@yourdomain.com
RESEND_SEND_TIMEOUT_MS=20000

# =====================================
# Common Email Settings
# =====================================
EMAIL_FROM="Kacha HR <onboarding@yourdomain.com>"  # fallback sender used by all providers

# =====================================
# REDIS CONFIGURATION
# =====================================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=redis_password_change_me